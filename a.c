#include <stdio.h>
#include <stdlib.h>

/**
 * main - prints the phrase "Programming is like building a multilingual puzzle
 * Return: 0 if exited properly, non-zero otherwise
 */
int main(void) {
    printf("O_RDONLY & O_WRONLY is %d", O_RDONLY & O_WRONLY);
    printf("\n=============\n");
    return (0);
}
